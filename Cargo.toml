[package]
name = "angle"
version = "0.5.1"
authors = ["ecoal95 <ecoal95@gmail.com>"]
description = "GLSL parsing and validation library based on ANGLE"
license = "MIT"

build = "build.rs"

[dependencies]
lazy_static = "1"

[build-dependencies]
cmake = "0.1"
